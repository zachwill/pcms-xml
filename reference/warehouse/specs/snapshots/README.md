# Snapshots — `reference/warehouse/specs/snapshots/`

These docs describe **saved view / snapshot worksheets** (or very-light variants) that don’t introduce new primitives.

When in doubt, prefer the canonical specs in the parent folder:
- `playground.md`
- `team.md`
- `patterns.md`
